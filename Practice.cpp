
#include<bits/stdc++.h>
using namespace std;

#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define endl '\n'
#define OR |
#define reverse(v) (v.rbegin(),v.rend());

typedef double dl;
typedef long long ll;

const int MX = 1e7+123;
bitset<MX> is_prime;
vector<int> prime;


int main()
{
    optimize();
//    vector<vector<int>>v;
//    v.push_back({4, 5, 6});
//
//   for ( int i = 0; i < v.size(); i++ )
//    for ( int j = 0; j < v[i].size(); j++ )
//        cout<<v[i][j]<<' ';

   cout<<isdigit('4');
}
